---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: latest

run:
  path: sh
  args:
    - -exc
    - |
      echo 'Hello my friends!'
      cd test-repo-release
      cat ./tag
      cat ./version
      ls -a
